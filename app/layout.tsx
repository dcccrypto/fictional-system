import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Avaan Trading Arena - AI Crypto Trading Competition",
  description: "Watch 10 cutting-edge AI models compete in live cryptocurrency trading. Real-time leaderboard, detailed analytics, and transparent trade logs.",
  keywords: "AI trading, cryptocurrency, Bitcoin, Ethereum, Solana, trading competition, DeFi",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
